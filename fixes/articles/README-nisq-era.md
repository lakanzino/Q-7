# بستهٔ انتشار مقاله «دوران NISQ» — Qpedia

فایل‌های این پوشه:

| فایل | چیست |
|---|---|
| `nisq-era.html` | متن آمادهٔ مقاله؛ فقط بدنه، بدون `<h1>`، بدون `<style>`، بدون `<script>`، بدون `&` خام |
| `nisq-era-featured.webp` | تصویر شاخص، دقیقاً ۱۲۰۰×۶۷۵، ‏33 KB |
| `nisq-era-featured.jpg` | همان تصویر با فرمت JPG، ‏81 KB |

بدنه فقط از تگ‌های `<p>`، `<h2 class="wp-block-heading">`، `<ul class="wp-block-list">`، `<ol class="wp-block-list">`، `<blockquote>`، `<details>` و سه کلاسِ از‌قبل‌موجودِ پوسته ساخته شده: `.qp-callout` + `.qp-callout__title` (باکس «پاسخ کوتاه»)، `.qp-narrative-box` (باکس کرمِ روایت بزرگان علم)، پس همان ظاهری را می‌گیرد که مقاله‌های انتشاریافتهٔ سایت دارند.

---

## ۱) فیلدهای انتشار

- **عنوان (H1، ‏۵۹ نویسه):** دوران NISQ چیست؟ رایانه‌های کوانتومیِ امروز کجا ایستاده‌اند
- **اسلاگ:** `nisq-era` ← همین آدرس در برگهٔ «شروع اینجاست» قبلاً ۴۰۴ می‌داد؛ با انتشار این مقاله زنده می‌شود.
- **دسته‌بندی (`quantum_category`):** «رایانش و ارتباطات کوانتومی» — slug: `quantum-computing` (id ۹۱، فرزندِ «فناوری و کاربردهای کوانتومی»). همان فرزند را بزنید، والد را نه.
- **برچسب‌ها:** فقط از برچسب‌های موجود انتخاب کنید (تایپ کنید و از لیست خودکار بردارید): «کیوبیت»، «رایانش کوانتومی»، «تصحیح خطای کوانتومی»، «برتری کوانتومی». برچسبِ تازه نسازید.
- **توضیح متا (۱۴۵ نویسه، سقف ۱۵۵ رعایت شده):**

  `دوران NISQ چیست؟ رایانه‌های کوانتومیِ نویزی با ده‌ها تا چند صد کیوبیت، محدودیتِ عمقِ مدار، و اینکه چرا تعداد کیوبیت تنها معیار نیست. با منابع اصلی.`

- **چکیده (excerpt):** نام NISQ را جان پرسکیل در ۲۰۱۸ پیشنهاد کرد: دورهٔ رایانه‌های کوانتومیِ ده‌تا‌چندصدکیوبیتی که هیچ لایهٔ تصحیح خطا ندارند. در این مقاله می‌خوانیم چرا در این دوران عمقِ مدار مهم‌تر از تعداد کیوبیت است، و یک ادعای کوانتومی را چطور باید سنجید.

## ۲) تصویر شاخص

- **ایدهٔ تصویری اختصاصی:** شبکه‌ای منظم از کره‌های کیوبیت که هرچه به سمتِ راست می‌رویم از کوک می‌افتد: حلقه‌ها کج می‌شوند، پیوندها می‌شکنند و به نقطه و موج‌های ریزِ نویز تبدیل می‌شوند؛ دو کرهٔ سمتِ راست خاموش‌اند. همان جملهٔ مقاله در قالب تصویر: مشکلِ این نسل «تعداد» نیست، «نویز» است.
- **سبک بصری:** وکتور تخت و مینیمال، جلدِ ژورنالِ علمی، با پالت خودِ سایت: زمینه `#E8F6FF`، کره‌ها `#0B3D91`، حلقه‌ها `#3BA7F2`، نویزها `#7FE7D6` و `#F4A261`؛ فضای خالیِ بالا برای تیتر.
- **نسبت ۱۶:۹ / ابعاد:** ۱۲۰۰×۶۷۵ (حداقلِ خواسته‌شده رعایت شده)
- **نام فایل انگلیسی:** `nisq-era-featured.webp`
- **ALT فارسی:** شبکهٔ کیوبیت‌هایی که از چپ منظم‌اند و به سمت راست با نویز از کوک می‌افتند؛ تصویرسازی دوران NISQ
- **عنوان (Title):** محدودیتِ دوران NISQ: نویز، نه تعداد کیوبیت
- **کپشن:** در دوران NISQ آنچه اجرا را ممکن یا ناممکن می‌کند، طولِ مدار و دقتِ دروازه است، نه تعداد کیوبیت.
- **توضیح (Description):** تصویرسازی انتزاعی از یک آرایهٔ کیوبیت؛ بی‌نظمیِ فزاینده از چپ به راست، محدودیتِ اصلیِ رایانه‌های کوانتومیِ کنونی را نشان می‌دهد.
- **منبع یا روش تولید:** تصویرسازیِ تولیدشده با مدل و بازبینی‌شده: هیچ فرمول، نماد علمی، عدد یا نوشته‌ای در تصویر نیست، پس خطرِ «نمادِ نادرست» ندارد و مجوزِ بیرونی لازم ندارد.

**پیشنهادِ تصویرِ آموزشیِ داخل مقاله (اختیاری، برای انتشارِ بعدی):** یک نمودارِ میله‌ای «شانسِ موفقیتِ مدار» به ازای عمقِ مدار، با همین چهار عدد که در متن محاسبه شده‌اند و هیچ داده‌ای به آن اضافه نمی‌شود:

| دقتِ هر دروازه | ۱۰۰ دروازه | ۱۰۰۰ دروازه |
|---|---|---|
| ۹۹٪ | ۳۷٪ | ۰٫۰۰۴٪ |
| ۹۹٫۵٪ | ۶۱٪ | ۰٫۷٪ |
| ۹۹٫۹٪ | ۹۰٪ | ۳۷٪ |

محلِ درج: بی‌درنگ پس از بندِ «دوم. خطاها میانگین نمی‌گیرند؛ ضرب می‌شوند.» — ALT پیشنهادی: «نمودار میله‌ای: با یک درصدِ خطای بیشتر، طولِ مدارِ قابل‌اعتماد چند برابر می‌شود» — کپشن: «اعداد از رابطهٔ (دقت) به توانِ (تعداد دروازه) به دست آمده‌اند، نه از یک دستگاهِ خاص.» بازبینیِ علمی: فقط تایپوگرافیِ محورِ فارسی؛ خودِ اعداد با ماشین‌حساب قابلِ بازتولیدند. نگاشتِ این نمودار در فایلِ HTML انجام **نشده** است، چون بی‌نمودارِ صادقانه بهتر از نمودارِ با محورِ حدسی است.

## ۳) روش درج (موبایل، از سی‌پنل یا پیش‌خوان)

1. پیش‌خوان وردپرس ← بخشِ مقاله‌های همین سایت (نوعِ نوشته `quantum_article`) ← **افزودن جدید**.
2. عنوان و اسلاگ جدول بالا را وارد کنید؛ دسته و برچسب را بزنید؛ توضیحِ متا و چکیده را هم.
3. **تصویر شاخص:** نوار کنار ← «تنظیم تصویر شاخص» ← بارگذاری ← `nisq-era-featured.webp` ← در فرم پیوست، ALT و Title و کپشنِ جدول بالا را وارد کنید.
4. محتوای `nisq-era.html` را بچسبانید:
   - ویرایشگرِ قدیمی (Classic): در تب **«متن»**، نه تب «بصری».
   - گوتنبرگ: یک بلوک **«HTML سفارشی» (Custom HTML)** بسازید و کل فایل را داخل همان **یک** بلوک بچسبانید؛ چند بلوک نسازید.
5. پیش‌نمایش را باز کنید و این پنج مورد را چشمی چک کنید: باکس آبیِ «پاسخ کوتاه»، باکس کرمِ نقل‌قولِ پرسکیل، فهرستِ شماره‌گذاری‌شدهٔ منابع، باز‌شدنِ پنج سؤالِ FAQ با یک لمس، و رنگِ آبیِ لینک‌های داخلی.
6. انتشار ← بعد از آن از «پاک‌سازی کش» لایت‌اسپید فقط همین یک آدرس را پرج کنید.

**دو هشدار:**
- این فایل فقط برای **بدنهٔ یک مقاله** است. در برگهٔ اصلیِ سایت یا در بلوک‌های پوسته چسبانده نشود.
- اگر خواستید چیزی به فایل اضافه کنید، `<style>`، `<script>` یا `&` خام اضافه **نکنید**؛ اگر ظاهر بهم ریخت، همان افزوده را بردارید، نه کل بلوک را.

**اختیاری ۱ — برگهٔ «شروع اینجاست»:** در `fixes/pages/start-page.html` بعد از سطرِ `/quantum-supremacy/` این یک سطر را اضافه کنید (یا در ویرایشگرِ برگه، به همان فهرست):

```html
<li><a href="/nisq-era/">دوران NISQ و محدودیت‌های رایانه‌های کوانتومی کنونی</a> — امروز دقیقاً کجا ایستاده‌ایم.</li>
```

**اختیاری ۲ — Schema پرسش‌های متداول:** اگر افزونهٔ سئو فیلدِ JSON-LDِ دستی دارد، این را در **همان فیلد** بگذارید، نه در بلوکِ HTML مقاله:

```json
{"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"NISQ یعنی چند کیوبیت؟","acceptedAnswer":{"@type":"Answer","text":"عدد ثابتی نیست. پرسکیل در ۲۰۱۸ بازهٔ ۵۰ تا ۱۰۰ کیوبیت را مثال زد و امروز عرفاً به هر دستگاهِ بدونِ تصحیح خطا از چند ده تا چند صد کیوبیت NISQ می‌گویند."}},{"@type":"Question","name":"چرا دوران می‌گویند و نه نسل؟","acceptedAnswer":{"@type":"Answer","text":"چون نسل وعدهٔ جایگزینی می‌دهد؛ دوران یعنی دوره‌ای که کار کردن در آن ممکن است."}},{"@type":"Question","name":"آیا رایانه‌های کوانتومی امروز کاری از دستشان برنمی‌آید؟","acceptedAnswer":{"@type":"Answer","text":"برعکس؛ فیزیک چندبدنی را اندازه می‌گیرند، الگوریتم ترکیبی را آزمایش می‌کنند و بنچ‌مارک می‌سازند. مسئله‌های صنعتیِ عمیق را نه."}},{"@type":"Question","name":"چرا تعداد کیوبیت را تبلیغ می‌کنند؟","acceptedAnswer":{"@type":"Answer","text":"چون عددِ کیوبیت ساده و قابلِ مقایسه است؛ دقتِ دروازه و عمقِ مجازِ مدار نه."}},{"@type":"Question","name":"پایان دوران NISQ چه شکلی است؟","acceptedAnswer":{"@type":"Answer","text":"شکستنِ قابلِ تکرارِ آستانهٔ تصحیح خطا، کیوبیتِ منطقیِ پایدار به تعداد زیاد، و یک کاربردِ واقعی با ارجاعِ قابلِ بررسی."}}]}
```

---

## ۴) گزارش کنترل نویسنده

- **تعداد کلمات:** ۱۶۶۷ کلمه برای بدنه تا پیشِ «مقاله‌های مرتبط»؛ با فهرستِ مرتبط‌ها، منابع و FAQ جمعاً ۲۲۳. بازهٔ دستورالعمل (۱۲۰۰ تا ۱۸۰۰) برای متنِ اصلی رعایت شده و بخش‌های الحاقی شمارش جدا دارند.
- **تعداد لینک‌های داخلی:** ۱۲ لینک به ۹ مقاله، همه زنده (با RESTِ سایت بررسی شد): `qubit` · `decoherence` (×۲) · `quantum-error-correction` (×۲) · `ibm-condor-processor` (×۲) · `post-quantum-cryptography` · `harvest-now-decrypt-later` · `q-day` · `quantum-supremacy` · `quantum-annealing-dwave`. سه نوعِ لازم هم پوشش داده شده: پایه (`qubit`)، تخصصی (`quantum-error-correction`، `ibm-condor-processor`)، تاریخی/انتقادی (`q-day`، `quantum-supremacy`).
- **تعداد منابع:** ۶، همه با DOI یا arXiv. چکیدهٔ منبع ۱ (پرسکیل) و شمارهٔ منابع ۳ و ۴ و ۵ِ همان مقاله در این نشست از سرورِ خودِ ژورنال خوانده و تطبیق داده شد؛ منابع ۵ و ۶ عیناً در فهرستِ منابعِ مقاله‌های خودِ سایت هم آمده‌اند (`q-day` برای Gidney و Ekerå و Google/Willow، و `quantum-computer-reality` برای نشانیِ Natureِ همان مقالهٔ ۲۰۴)؛ منبع ۲ هم در `quantum-supremacy` با نشانیِ nature.com ارجاع شده، پس هیچ‌کدام از شش منبع از خودِ سازندهٔ متن نیست.
- **تعداد تصاویر پیشنهادی:** ۱ تصویر شاخص (ساخته و تحویل شد) + ۱ نمودارِ آموزشیِ اختیاری که داده‌اش در جدول بالا آمده و عمداً داخلِ HTML گذاشته نشد.
- **مهم‌ترین سوءبرداشت اصلاح‌شده:** «تعدادِ کیوبیت، قدرت را می‌رساند». جایگزینِ مقاله: حاصل‌ضربِ دقتِ عملیات × اتصال × عمقِ مجازِ مدار؛ و یک محاسبهٔ کاغذی که خواننده خودش بازتولید می‌کند (۹۹٪ در ۱۰۰ دروازه ≈ ۳۷٪ موفقیت؛ ۹۹٫۵٪ همان اطمینان را در حدود ۲۰۰ دروازه می‌دهد).
- **مهم‌ترین محدودیت علمی:** نه تعریفِ اجماع‌شده‌ای از «کاربردِ مفید» وجود دارد و نه گزارشِ آستانهٔ ۲۰۲۴ روی سخت‌افزارِ مستقلِ دیگری تکرار شده؛ مقاله به‌جای نتیجه‌گیریِ قطعی، همین را در بخشِ «چه چیزهایی را هنوز نمی‌دانیم؟» گذاشته است.
- **آیا مقاله ادعای اثبات‌نشده دارد؟** نه در شکلِ قطعی. تنها جمله‌ای که یک بازهٔ عددیِ بدونِ منبعِ مستقیم دارد «دقتِ دروازهٔ دوکیوبیتی جایی بین ۹۹ تا ۹۹٫۹٪ گزارش شده است» است که به‌صورتِ گزارش، نه قانون، نوشته شده. عددِ «بیست میلیون کیوبیتِ نویزی» به منبع ۵ و «تراشهٔ ۱۰۵ کیوبیتیِ ۲۰۲۴» به منبع ۶ بسته شده است.
- **آیا بازبینی متخصص لازم است؟** دو بازبینیِ یک‌دقیقه‌ای: (۱) بازکردنِ دستیِ شش لینکِ DOI/arXiv (در این محیط فقط `quantum-journal.org` زنده باز شد)، (۲) اگر خواستید جملهٔ بازهٔ دقت محکم‌تر شود، «در بیشترِ دستگاه‌های این نسل…» را به «در مقالهٔ پرسکیل و گزارش‌های پس از آن…» تغییر دهید.
- **امتیاز اولیه از ۱۰۰:** ۹۲؛ دو نمره به‌خاطرِ همان بازهٔ عددیِ بدونِ منبعِ مستقیم و یکی به‌خاطرِ اینکه «هشت سال است» وابسته به تاریخِ انتشار است.
- **سه جمله‌ای که برای انسانی‌تر شدن بازنویسی کردم:**
  1. «سازها یکی‌یکی می‌نوازند» ← «سازها هم‌زمان می‌نوازند» — تشبیه اول با خودِ موضوعِ مقاله (موازی‌بودنِ محاسبه) می‌جنگید.
  2. «مثل شمعدانی در اتاقی است که اکسیژنش دارد تمام می‌شود: تعداد مهم است، ولی بدونِ آن یکی دیگر، هیچ نورِ بیشتری نمی‌دهد» ← «مثل شمعی است که در اتاقی بدونِ اکسیژن روشنش کرده‌اند: تعداد مهم است، ولی بدونِ آن اکسیژن، هیچ‌کدام نورِ بیشتری نمی‌دهد» — معلوم نبود «آن یکی دیگر» به چه اشاره می‌کند.
  3. «نه بدبینیِ راحت اینجا کار می‌کند و نه خوش‌بینیِ بی‌پایه» ← «نه بدبینیِ راحت اینجا جواب می‌دهد و نه خوش‌بینیِ بی‌پایه؛ کارِ ما فقط جداکردنِ عدد از حدس است» — فعلِ «کار می‌کند» بویِ ترجمهٔ ماشینی می‌داد.
  - سه اصلاحِ دیگر هم انجام شد: «شش سال» ← «هشت سال» (۲۰۱۸ تا ۲۰۲۶)، «brochure» ← «کاتالوگ»، و حذفِ «نمی‌فروشیم» از جملهٔ خودِ معرفی (واژهٔ فروش در لحنِ ممنوعِ ماست).

---

## ۵) خروجیِ «فیلتر نهایی تأیید انتشار»

وضعیت نهایی: **PASS** (پیش از آن یک دور REVIEW خورد و اصلاحاتش روی همین فایل اعمال شد)

| سنجه | امتیاز |
|---|---|
| امتیاز صدای اختصاصی Qpedia | ۴۷ از ۵۰ (آستانهٔ رد: ۴۰) |
| امتیاز علمی | ۹۰ از ۱۰۰ |
| امتیاز محتوایی | ۹۳ از ۱۰۰ |
| امتیاز SEO | ۹۱ از ۱۰۰ |
| امتیاز خوانایی | ۹۲ از ۱۰۰ |
| امتیاز تصاویر | ۹۰ از ۱۰۰ |

ده‌گانهِ امضا، تک‌تک: لحن مهربان و معلم‌گونه ۵ · صمیمیت کنترل‌شده ۵ · توضیح انسانی و غیرکلیشه‌ای ۵ · تنوع ریتم و طول جمله ۴ (پاراگراف‌ها بین ۱۲ تا ۸۹ کلمه؛ طول جمله‌های آغازین: ۲۶، ۱۷، ۳۱، ۲۷) · محاورهٔ طبیعی و محدود ۵ · وضوح تفاوت تشبیه و واقعیت ۵ (جملهٔ «اما حواسمان باشد این فقط یک تشبیه است، نه خودِ واقعیت» عیناً آمده) · صداقت دربارهٔ محدودیت علم ۵ · اختصاصی‌بودن زاویه ۴ · نبودِ عبارت کلیشه‌ای ۵ · قابل‌شناسایی‌بودن صدای Qpedia ۴. جمع: ۴۷ — بالای آستانه، پس رد قطعی نیست.

**سه ویژگی انسانی و موفق مقاله:**
1. دو جمله‌ای که هیچ مقالهٔ دیگری نمی‌تواند عیناً کپی‌شان کند: «وقتی داورِ مسابقه ندارید، باید خودتان خطکش بسازید» و «در این دوره، کوچک و قابل‌بررسی شریف‌تر از بزرگ و بی‌آزمون است».
2. نقل‌قولِ پرسکیل در باکسِ مخصوصِ روایت بزرگان علم نشسته و بی‌پرده می‌گوید «این ترجمهٔ جمله‌ای از چکیده است، نقل‌قولِ آزاد نیست» و خواننده را به مقایسه با «will not change the world right away» دعوت می‌کند.
3. خواننده با دو عدد بیرون می‌آید که خودش با ماشین‌حساب بازتولیدشان می‌کند (۳۷٪ و حدود ۲۰۰ دروازه)، نه با یک ادعای آماده.

**پنج اصلاح ضروری (سه‌تایشان همین حالا روی همین فایل اعمال شده؛ دو موردِ آخر فقط دستِ سردبیر است):**
1. ✅ مقدمه از ۲۰۱ کلمه به ۱۲۸ رسید تا در بازهٔ خواسته‌شده (۸۰ تا ۱۴۰) بماند و نقل‌قول از مقدمه به باکسِ روایت رفت.
2. ✅ جمع‌بندی از «یک بند ۱۰۲ کلمه‌ای» به چهار بند کوتاه به‌علاوهٔ یک نقل‌قولِ تأکیدی تبدیل شد؛ کلاسِ `.qp-narrative-box` هم از جمع‌بندی برداشته شد، چون در پوسته فقط برای «روایتِ شخصیِ بزرگان علم» تعریف شده بود.
3. ✅ در بخشِ ندانسته‌ها، دلیلِ پراکندگیِ تخمین‌ها «منابعِ کلاسیکیِ کالیبراسیون» نوشته شده بود که دقیق نیست؛ به «وابستگی به الگوریتم و به کیفیتِ خطا» عوض شد.
4. ⬜ شش لینکِ DOI/arXiv را یک‌بار دستی باز کنید؛ اگر هرکدام تغییر آدرس داد، همان یک خطِ منبع را اصلاح کنید.
5. ⬜ اگر می‌خواهید تیترِ FAQ با ۱۶۱ مقالهٔ دیگر سایت یکسان باشد، `<h2 class="wp-block-heading">پرسش‌های متداول (FAQ)</h2>` را به `<h2 class="wp-block-heading">سوالات متداول</h2>` تغییر دهید. ساختارِ `<details><summary>` که در مقاله‌های سایت رایج است، دست‌نخورده مانده.

**جمله‌هایی که بویِ متنِ خامِ هوش مصنوعی می‌دادند و بازنویسی شدند:**
- «شگفت‌انگیز است که رایانه‌های کوانتومی در حال تغییر دنیا هستند» → «وقتی دستگاه‌ها از مرزِ هزار کیوبیت گذشتند، میانه معنای عددی‌اش را از دست داد.»
- «این فناوری می‌تواند در آینده صنایع بسیاری را متحول کند» → «ارزشِ اصلیِ این نسل، پیش از هر کاربردِ صنعتی، اندازه‌گیریِ فیزیک است.»
- «به‌طور خلاصه، NISQ مرحله‌ای ضروری است» → «NISQ نه وعده‌ای است که پشتِ سرمان افتاده و نه رؤیایی که جلوی چشم؛ دوره‌ای است که در آن یاد گرفته‌ایم سؤال‌هایی بپرسیم که هنوز راهِ بررسی‌شان را بلد نیستیم.»

**قوانینِ ردِّ قطعی، بند‌به‌بند:** خطای علمیِ جدی: ندارد · منبعِ جعلی یا غیرقابل‌بررسی: ندارد (منبع ۱ زنده خوانده شد؛ بقیه DOI/arXiv دارند) · حداقل ۳ منبع: ۶ ✓ · حداقل ۳ لینک داخلی: ۱۲ ✓ · ALTِ تصویر شاخص: دارد ✓ · لحن کاملاً ماشینی یا تبلیغاتی: ندارد · ادعای قطعی دربارهٔ موضوعِ اثبات‌نشده: ندارد (هر سه جای حساس به بخشِ «هنوز نمی‌دانیم» منتقل شده) · استفادهٔ افراطی از محاوره: ندارد · نبودِ توضیح دربارهٔ محدودیتِ تشبیه‌ها: ندارد، تشبیه ارکستر بلافاصله مرزهایش را می‌گوید ✓

**تصمیم انتشار: PASS**
