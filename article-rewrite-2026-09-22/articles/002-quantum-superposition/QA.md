# کنترل کیفیت مقاله ۰۰۲

- شماره: ۲ از ۳۱۰
- Post ID: `287`
- نامک: `quantum-superposition`
- وضعیت: بازنویسی و کنترل شد

## کنترل ساختار

- تیتر ممنوع «مقدمه انسانی» حذف شد و متن با پاراگراف مقدمه آغاز می‌شود.
- H1 داخل محتوا درج نشده است؛ عنوان نوشته در قالب WordPress نقش H1 دارد.
- عنوان WordPress و عنوان SEO یکسان نیستند.
- همه styleهای inline از figure، تصویر و توضیح تصویر حذف شدند.
- تگ‌های `style`، `script`، `details` و `summary` وجود ندارند.
- FAQ پیش از منابع قرار دارد و سؤال‌ها با `<strong>` در پاراگراف مستقل آمده‌اند.
- منابع آخرین بخش مقاله‌اند و پس از آن محتوایی نیست.
- تمام مقصدهای لینک داخلی نسخه اصلی حفظ شده‌اند.
- URL تصویر نسخه اصلی حفظ شده و alt آن اصلاح نگارشی شده است.

## کنترل علمی

- ادعای نادرست «آزمون بل همه حالت‌های پنهان را رد کرده» به «متغیرهای پنهان محلی» محدود شد.
- برهم‌نهی از مخلوط آماری با معیار تداخل و رابطه فازی تفکیک شد.
- توضیح واهمدوسی اصلاح شد: واهمدوسی مشاهده تداخل را دشوار می‌کند، اما به‌تنهایی مسئله نتیجه یگانه را حل نمی‌کند.
- تعبیر «هر برهم‌کنش یک اندازه‌گیری کامل است» حذف شد.
- تعبیر رایج اما گمراه‌کننده «۲^N محاسبه هم‌زمان» اصلاح شد؛ فقط فضای دامنه‌ها و نقش تداخل توضیح داده شد.
- اعداد بدون منبع درباره دمای لازم برای اتم، جسم میکرومتری و سیب حذف شدند.
- ادعاهای کلی درباره لیزر و پیوند شیمیایی به‌دلیل نبود منبع تخصصی، صریحاً نیازمند منبع اعلام شدند.
- ادعای «برهم‌نهی خود واقعیت است» به بیان غیرتفسیری و قابل‌آزمون تبدیل شد.

## وضعیت منابع و لینک‌ها

- منبع Feynman تنها نشانی متنی موجود در منابع نسخه اصلی بود؛ همان مقصد به لینک فعال HTTPS تبدیل و با ابزار شبکه بازیابی شد.
- برای سه منبع چاپی دیگر URL تازه ساخته نشد.
- هر ده مقصد داخلی با موجودی رسمی `articles-001-310.csv` تطبیق داده شدند.

## بازبینی برای مخاطب عمومی

- برهم‌نهی با دو بلندگو و تداخل با موج‌های آب توضیح داده شد.
- فضای حالت به «نقشه حالت‌ها» و فاز به هماهنگی دست‌زدن دو گروه تشبیه شد.
- نقش پایه اندازه‌گیری با پرسش‌های متفاوت از قطب‌نما توضیح داده شد.
- واهمدوسی با محوشدن نقش موج روی آب زیر باران توضیح داده شد.
- محدودیت خواندن خروجی کوانتومی با کتابخانه‌ای که تنها یک کتاب از آن خارج می‌شود توضیح داده شد.
- مرز و نقطه شکست همه مثال‌ها بلافاصله ذکر شد.
- FAQ از چهار پرسش به پنج پرسش افزایش یافت و متن آن برای Schema آینده آماده است.

## مهارت‌های استفاده‌شده

- `no-ai-slop`: حذف تیتر و عبارت‌های ماشینی، تکرار و قطعیت‌های نمایشی.
- `professional-proofreader`: ویرایش فارسی، نیم‌فاصله و انسجام.
- `seo-structure-architect`: بازسازی سلسله‌مراتب H2/H3 و جایگاه FAQ و منابع.
- `seo-content-auditor`: کنترل عنوان، ساختار و آمادگی انتشار.

هیچ مهارت `critical` یا `offensive` استفاده نشد.
