# راهنمای تکمیلی ساختار مقالات Qpedia

این سند مکمل «پرامپت نویسنده اختصاصی Qpedia» است و در صورت تعارض، قواعد این سند برای مقاله‌های جدید اولویت دارد.

## ترتیب و تنوع ساختار

1. پس از H1، مقدمه مستقیماً و بدون هیچ تیتر واسط آغاز شود.
2. استفاده از تیتر «مقدمه انسانی» در متن مقاله ممنوع است.
3. تیترهای همه مقاله‌ها نباید عیناً یکسان باشند. ساختار هر مقاله باید با پرسش و منطق همان موضوع طراحی شود.
4. بخش پاسخ سریع می‌تواند وجود داشته باشد، اما نام و قالب آن متناسب با موضوع تغییر کند.
5. منابع معتبر همیشه آخرین بخش مقاله باشند؛ بعد از منابع هیچ FAQ، گزارش سردبیری یا محتوای قابل‌انتشار دیگری قرار نگیرد.
6. اطلاعات مدیریتی، گزارش کنترل و داده‌های تصویر خارج از متن قابل‌انتشار و در سند یا فیلد جدا نگهداری شوند.

## تنوع بصری و بلاک‌ها

1. برای شکستن یکنواختی متن، در جای مناسب از بلاک نکته، هشدار علمی، فرمول، جدول یا نقل‌قول استفاده شود؛ نه صرفاً برای تزئین.
2. فرمول‌های تکمیلی که برای فهم عمومی ضروری نیستند، در یک باکس رنگی با توضیح کوتاه نمادها ارائه شوند تا برای خواننده متخصص نیز مفید باشند.
3. جدول فقط وقتی استفاده شود که مقایسه یا دسته‌بندی را واقعاً روشن‌تر کند.
4. نقل‌قول مستقیم تنها وقتی مجاز است که متن دقیق آن از منبع اصلی معتبر بررسی شده و لینک همان منبع درج شده باشد. در نبود امکان تطبیق واژه‌به‌واژه، مفهوم منبع بازنویسی شود و داخل گیومه قرار نگیرد.
5. بلاک‌ها باید خوانایی موبایل، راست‌چین بودن و کنتراست مناسب را حفظ کنند.

## پرسش‌های متداول با HTML ساده

1. FAQ نباید شامل `style`، CSS درون‌خطی، `details`، `summary` یا ساختار آکاردئونی باشد؛ ویرایشگر سایت این استایل‌ها را به متن قابل‌مشاهده تبدیل می‌کند.
2. هر سؤال فقط با HTML ساده و تگ `<strong>` بولد شود.
3. پاسخ هر سؤال در پاراگراف معمولی و بلافاصله پس از سؤال قرار گیرد.
4. پاسخ‌ها کوتاه، غیرتکراری و مستقیماً مرتبط با قصد جست‌وجوی کاربر باشند.
5. برای هر مقاله FAQ Schema از نوع `FAQPage` هنگام ساخت افزونه درون‌ریز تولید شود؛ متن اسکیما باید دقیقاً با FAQ قابل‌مشاهده یکسان باشد.

## کنترل نهایی الزامی

- فقط یک H1 وجود دارد.
- مقدمه بدون تیتر شروع می‌شود.
- نام تیترها متناسب با موضوع و غیرقالبی است.
- سؤال‌های FAQ با `<strong>` بولد شده‌اند و هیچ CSS یا آکاردئونی در محتوا وجود ندارد.
- منابع آخرین بخش مقاله‌اند.
- نقل‌قول بررسی‌نشده وجود ندارد.
- عنوان، نویسنده، نشریه و DOI یا URL همه منابع با صفحه ناشر یا پایگاه معتبر تطبیق داده شده‌اند.
- لینک منابع پیش از پوش بررسی شده و از لینک جعلی، ناقص یا نامرتبط استفاده نشده است.
- فرمول، جدول و بلاک‌های رنگی از نظر علمی و نمایش موبایل بررسی شده‌اند.
