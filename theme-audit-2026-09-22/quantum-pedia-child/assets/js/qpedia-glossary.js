(()=>{'use strict';let active=null,pop=null,lastFocus=null;const margin=10,gap=10;
function makePopover(){if(pop)return pop;pop=document.createElement('aside');pop.className='qpedia-glossary-popover';pop.setAttribute('role','dialog');pop.setAttribute('aria-modal','false');pop.setAttribute('aria-hidden','true');pop.innerHTML='<div class="qpedia-glossary-popover__head"><strong class="qpedia-glossary-popover__title"></strong><button class="qpedia-glossary-popover__close" type="button" aria-label="بستن تعریف">×</button></div><p class="qpedia-glossary-popover__definition"></p><a class="qpedia-glossary-popover__more" href="#">مطالعه بیشتر ←</a>';document.body.appendChild(pop);pop.querySelector('.qpedia-glossary-popover__close').addEventListener('click',()=>close(true));return pop}
function place(){if(!active||!pop)return;const r=active.getBoundingClientRect(),pr=pop.getBoundingClientRect(),vw=document.documentElement.clientWidth;let left=r.left+r.width/2-pr.width/2;left=Math.max(margin,Math.min(left,vw-pr.width-margin));const roomTop=r.top,side=roomTop>pr.height+gap+margin?'top':'bottom';const top=side==='top'?scrollY+r.top-pr.height-gap:scrollY+r.bottom+gap;const arrow=Math.max(14,Math.min(r.left+r.width/2-left,pr.width-14));pop.dataset.side=side;pop.style.left=Math.round(scrollX+left)+'px';pop.style.top=Math.round(top)+'px';pop.style.setProperty('--qpedia-glossary-arrow-x',arrow+'px')}
function open(term){if(active&&active!==term)active.setAttribute('aria-expanded','false');active=term;lastFocus=term;const p=makePopover();p.querySelector('.qpedia-glossary-popover__title').textContent=term.textContent.trim();p.querySelector('.qpedia-glossary-popover__definition').textContent=term.dataset.glossaryDefinition||'';const more=p.querySelector('.qpedia-glossary-popover__more'),url=term.dataset.glossaryUrl;if(url){more.href=url;more.hidden=false}else more.hidden=true;term.setAttribute('aria-expanded','true');p.setAttribute('aria-hidden','false');p.classList.add('is-open');place()}
function close(restore=false){if(!active||!pop)return;active.setAttribute('aria-expanded','false');active=null;pop.classList.remove('is-open');pop.setAttribute('aria-hidden','true');if(restore&&lastFocus)lastFocus.focus({preventScroll:true})}
document.addEventListener('click',e=>{const term=e.target.closest('.qpedia-glossary-term');if(term){e.preventDefault();if(active===term)close(false);else open(term);return}if(pop&&pop.contains(e.target))return;close(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')close(true)});addEventListener('resize',()=>{if(active)place()},{passive:true});addEventListener('scroll',()=>{if(active)place()},{passive:true});
})();
