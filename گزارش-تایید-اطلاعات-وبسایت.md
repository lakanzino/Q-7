# گزارش تایید اطلاعات وب‌سایت qpedia.ir

تاریخ: ۱۵ شهریور ۱۴۰۵ (۲۰۲۶-۰۹-۱۵) — روش: تحلیل خروجی وردپرس + سنجش آنلاین (REST API عمومی)

## ۱. فایل‌های دریافت‌شده در گیت‌هاب ✅

| فایل | وضعیت |
|---|---|
| `# پرامپت نویسنده اختصاصی Qpedi.txt` (۴۰۵ خط) | کامل خوانده شد — شامل پرامپت نویسنده + فیلتر سردبیر نهایی |
| `WordPress.2026-09-14 all.xml` (۴/۳ مگابایت) | کامل تحلیل شد — خروجی کامل وردپرس |

## ۲. ساختار سایت (از داده واقعی)

- **وردپرس ۷.۱** + سرور/کش **LiteSpeed**
- **۴ نوع محتوا سفارشی (CPT):**
  - `quantum_article` — مقالات کوانتوم: **۲۰۶** (۲۰۱ انتشار‌یافته + ۵ سطل آشغال)
  - `quantum_scientist` — دانشمندان: **۵۶** (آرشیو: /scientists)
  - `glossary` — واژه‌نامه: **۳۳**
  - `page` — ۳ صفحه (صفحه اصلی page_id=2790)
- **۲۶۹** فایل رسانه (آپلود) — فرمت‌های webp در کار است
- **۱۳ اصطلاح دسته** در تاکسونومی سفارشی `quantum_category`
- صفحه «نوشته‌ها» (post) اصلاً فعال نیست — API آن خالی برمی‌گردد؛ همه محتوا CPT است

## ۳. مقایسه خروجی vs آنلاین (تایید)

| شاخص | خروجی (دیروز ۲۰۲۶-۰۹-۱۴) | آنلاین (امروز API) | ویجت صفحه اصلی |
|---|---|---|---|
| مقالات انتشار‌یافته | ۲۰۱ | **۱۸۷** | ۱۷۱ |
| جدیدترین مقاله‌ها | `mitochondria-proton-tunneling`، `dna-repair-enzymes`، `nobel-physics-2012` | ✅ یکسان | — |

**⚠️ سه عدد متفاوت:** ۲۰۱ (خروجی دیروز) ≠ ۱۸۷ (آنلاین امروز) ≠ ۱۷۱ (ویجت خانه).
یعنی ۱۴ مقاله بین دیروز و امروز وضعیتشان عوض شده و ویجت خانه ۱۶ عدد از واقعی کمتر است.
→ **نیاز به یک نگاه سریع به ادمین:** آیا ۱۴ مقاله un-publish یا حذف شده؟ ویجت خانه از چه منبعی عدد می‌گیرد؟

## ۴. یافته‌های فنی مهم (برای سند استانداردها)

1. **۴ سیستم متادیتای SEO هم‌زمان در داده‌ها:** `rank_math_*` (۱۵۷)، `_yoast_wpseo_*` (۱۵۷)، `_qpedia_seo_*` (۹۲)، `_jetica_*` (۷). باید مشخص شود کدام فعلاً فعال است؛ بقیه باید غیرفعال/پاک شوند.
2. **۸۲ مقاله (از ۲۰۶) هیچ متای SEO ندارند** (نه رنک‌میت، نه یوآست).
3. **سایت‌مپ عملیاتی نیست:** `/sitemap.xml` خالی و `/sitemap_index.xml` خطای ۴۰۴.
4. **۲۰۶ مقاله هیچ تصویر درون‌متنی ندارند** (فقط thumbnail). تصاویر آموزشی داخل متن = صفر.
5. **۳۵ مقاله بدون هیچ لینک داخلی**، **۵۸ مقاله بدون هیچ لینک بیرونی/منبع**.
6. **۱۲۰ مقاله چنددسته‌ای هستند** (استاندارد: یک دسته اصلی + حداکثر یک فرعی).
7. میانگین طول مقاله **۹۲۹ کلمه** (کمینه ۴۰۸، بیشینه ۱۷۵۵) — برای عمق موضوعات سنگین کوتاه است.
8. ۱۶۸ مقاله بخش «منابع» دارند؛ ۵۶ مقاله بخش FAQ.
9. اسلاگ‌ها همه انگلیسی و تمیز ✅، نیم‌فاصله در متن‌ها رعایت‌شده به‌نظر می‌رسد ✅

## ۵. نتیجه تایید

- دسترسی خواندن/نوشتن به گیت‌هاب ✅، خواندن سایت ✅ (API عمومی باز است — crawl کامل ممکن است)
- اطلاعات آپلود‌شده کافی برای شروع است؛ فقط ۲ نکته خالی: (الف) عدد صحیح مقالات و (ب) افزونه SEO فعال فعلی
